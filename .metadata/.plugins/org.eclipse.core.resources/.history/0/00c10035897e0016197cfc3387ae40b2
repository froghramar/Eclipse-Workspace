package problem1;

public class MyClass {
	
	public static int mod = 10007;
	
	public int bigmod(int n, int p) {
		if(p == 0) return 1;
		else if(p % 2 == 1) return (n * bigmod(n, p - 1)) % mod;
		else{
			int x = bigmod(n, p / 2);
			return (x * x) % mod;
		}
	}
}
